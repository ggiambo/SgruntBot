spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:sgrunt
    username: "sgrunt"
    password: "sgrunt"

logging:
  file:
    name:
  pattern:
    console: "%d [%thread] %-5level %logger{36} - %msg%n"

sgruntbot:
  config:
    chat-id: -676046724
    proxy: "http://host.containers.internal:8888"
    baseRestUrl: http://localhost:8081
